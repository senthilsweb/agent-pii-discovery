# Real PII never lands in this public repo. Patterns are anchored to the repo
# root: evals/data/ is the SYNTHETIC fixture corpus and is committed
# (see evals/corpus/generate.py — all values fabricated).
/uploads/
runs/
/data/
/exports/
*.duckdb
*.duckdb.wal

# MkDocs local build output
/site/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/
